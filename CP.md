the reccomented way to compile a cpp prgram

```c++
g++ -std=c++11 -O2 -Wall test.cpp -o test
```

The compiler follows the C++11 standard (-std=c++11),
optimizes the code (-O2) and
shows warnings about possible errors (-Wall).
